---

- name: Init state
  set_fact:
    cloud_networks_state: []

- name: start setup VPC for each provider
  include_tasks: "{{ item_vpc.provider }}-vpc.yml"
  with_items: "{{ cloud_networks | d([]) }}"
  loop_control:
    loop_var: item_vpc
